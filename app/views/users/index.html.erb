<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>

<h2>Users, not limited, works ok</h2>
<table>
	<thead>
		<th>Name</th>
	</thead>
	<tbody>
		<% @users.each do |user| %>
			<tr>
				<td><%= user.name %>
			</tr>
		<% end %>
	</tbody>
</table>

<%== pagy_nav(@pagy) %>

<h2>Users, uses #limit(2), does not work</h2>
<p>Returns up to Pagy::DEFAULT[:items] which is <%= Pagy::DEFAULT[:items] %></p>
<table>
	<thead>
		<th>Name</th>
	</thead>
	<tbody>
		<% @users_limited.each do |user| %>
			<tr>
				<td><%= user.name %>
			</tr>
		<% end %>
	</tbody>
</table>

<%== pagy_nav(@pagy_limited) %>
